- 所有 API 响应均包裹在 `{"data": ...}` 结构中，保持前端消费接口的一致性。
- 主键统一采用字符串类型的 UUID（`str(uuid.uuid4())`），并在模型层通过 `default` 回调自动生成。
- 时间戳字段（`created_at`, `updated_at`）在模型层通过 `lambda` 默认值自动维护 UTC 时间。
- 更新操作（PUT）普遍采用 `body.model_dump(exclude_none=True)` 配合 `setattr` 实现部分字段更新（Partial Update）。